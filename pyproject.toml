[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "passive-extended-matchgate-simulator"
requires-python = ">=3.8"
version = "0.1.0"
description = "Passive extended matchgate quantum circuit simulator"
readme = "README.md"
license = { file = "LICENSE" }

dependencies = [
  "numpy>=2.3.2",
  "scipy>=1.16.0",
  "qiskit>=2.0.2",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.4.0",
  "maturin>=1.0",
]

[tool.maturin]
features = ["pyo3/extension-module"]
python-source = "python"
module-name = "passive_extended_matchgate_simulator._lib"

[tool.ruff]
src = ["python"]
include = [
  "pyproject.toml",
  "python/**/*.py",
  "python/**/*.pyi",
  "tests/**/*.py",
  "tests/**/*.pyi",
  "docs/**/*.py",
  "docs/**/*.pyi",
  "docs/**/*.ipynb",
]

[tool.ruff.lint]
select = ["E", "F", "I", "N", "NPY", "NPY201"]
